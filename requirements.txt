# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Template rendering
jinja2>=3.1.0

# HTTP requests and data fetching (required for CVE data download)
requests>=2.28.0
httpx>=0.27.0  # Async HTTP client for parallel downloads

# Date and time handling
python-dateutil>=2.8.0

# JSON handling (built-in, but explicit for clarity)
# json - built-in

# File system operations (built-in)
# os, shutil - built-in

# For potential future CSV/Excel data sources
openpyxl>=3.1.0

# For potential future API integrations
urllib3>=1.26.0

# For data validation and cleaning
jsonschema>=4.17.0

# For potential future database connections

# CLI framework
typer>=0.9.0
rich>=13.0.0  # For rich terminal output with Typer

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0  # For async test support
pytest-cov>=4.0.0  # For coverage reports

# Code quality
mypy>=1.0.0  # Type checking
ruff>=0.1.0  # Fast Python linter (replaces flake8, black, isort)

# Pre-commit hooks
pre-commit>=3.0.0
